// Package fs provides functions for accessing the timetrace filesystem.
//
// Instead of juggling around with paths of project and record files yourself,
// you should rely on the fs package as an uniform interface for accessing the
// underlying files and directories.
package fs
